kind: Route
apiVersion: route.openshift.io/v1
metadata:
  name: designer
spec:
  to:
    kind: Service
    name: designer
  port:
    targetPort: http
---
apiVersion: v1
kind: Service
metadata:
  name: designer
  labels:
    app: designer
spec:
  ports:
    - port: 8080
      name: http
  selector:
    app: designer
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: designer
  labels:
    app: designer
spec:
  replicas: 1
  selector:
    matchLabels:
      app: designer
  template:
    metadata:
      labels:
        app: designer
    spec:
      containers:
        - name: app
          image: quay.io/jonkey/biz-platform-designer
          imagePullPolicy: Always
          ports:
            - containerPort: 8080
          env:
            - name: spring.cloud.consul.host
              value: consul-server
          livenessProbe:
            httpGet:
              path: /actuator/health
              port: 8080
              scheme: HTTP
          readinessProbe:
            httpGet:
              path: /actuator/health
              port: 8080
              scheme: HTTP
