kind: Route
apiVersion: route.openshift.io/v1
metadata:
  name: runtime
spec:
  to:
    kind: Service
    name: runtime
  port:
    targetPort: http
---
apiVersion: v1
kind: Service
metadata:
  name: runtime
  labels:
    app: runtime
spec:
  ports:
    - port: 8080
      name: http
  selector:
    app: runtime
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: runtime
  labels:
    app: runtime
spec:
  replicas: 1
  selector:
    matchLabels:
      app: runtime
  template:
    metadata:
      labels:
        app: runtime
    spec:
      containers:
        - name: app
          image: quay.io/jonkey/biz-platform-runtime
          imagePullPolicy: Always
          ports:
            - containerPort: 8080
          env:
            - name: spring.cloud.consul.host
              value: consul-server
          livenessProbe:
            httpGet:
              path: /actuator/health
              port: 8080
              scheme: HTTP
          readinessProbe:
            httpGet:
              path: /actuator/health
              port: 8080
              scheme: HTTP
